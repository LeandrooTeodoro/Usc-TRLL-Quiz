/**
 * Copie este arquivo para "config.local.js" (não versionado) e preencha com as
 * credenciais reais do projeto Supabase. Inclua <script src="config.local.js">
 * em index.html ANTES de js/supabase-client.js.
 *
 * Enquanto este arquivo não existir/for preenchido, o app roda inteiramente em
 * modo local (localStorage) — o que já atende ao uso offline em campo.
 */
window.TRLL_CONFIG = {
  SUPABASE_URL: "https://SEU-PROJETO.supabase.co",
  SUPABASE_ANON_KEY: "SUA-ANON-KEY-PUBLICA",
};
